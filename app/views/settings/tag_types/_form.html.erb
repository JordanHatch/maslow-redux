<%= semantic_form_for [:settings, tag_type] do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :show_index_page %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, as: :button, button_html: { class: 'btn btn-primary' } %>
  <% end %>
<% end %>
