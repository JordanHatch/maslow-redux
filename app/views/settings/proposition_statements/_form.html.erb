<%= semantic_form_for [:settings, proposition_statement] do |f| %>

  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :description %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit,  button_html: { class: 'btn btn-primary' } %>
  <% end %>

<% end %>
